# Data policy decisions

All application processing/sharing scopes default off. This scaffold collects no workload data. Synthetic fixtures must remain clearly marked and free of real employee records.

Before deployment, specify retention and deletion timelines, consent versioning, snapshot invalidation, export expiry, handling of historical exports, access to consent audit records, and backup deletion limitations. Optional notes must never enter manager aggregates or notification payloads.
