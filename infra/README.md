# Infrastructure

`pnpm infra:synth` synthesizes an empty CDK stack locally. It does not deploy AWS services. Authentication, DynamoDB, API routes, queues, buckets, and alarms are planned, not provisioned. Add reviewed resources here as features are implemented; isolate deployment environments.
