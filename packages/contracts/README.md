# @workload/contracts

Client-safe Zod runtime schemas and API types for consent, workload records, private items, sharing publications, aggregate states, insights, memberships, teams, and policies. All consent scopes default off. Schemas reject private-note sharing and enforce dated one-time private-item retention inputs; backend authorization remains mandatory.
